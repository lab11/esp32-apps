BLE Connect Example for Throughput Test
=======================================

Connects to a device and receives a large stream of data.

Specify device role (sending/receiving central device) in the `Component config > Device role` config menu.